<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tonjies.abase"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="28" />

    <application
        android:name="com.example.tonjies.abase.app.App"
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/NoActionBar" >
        <activity android:name="com.example.tonjies.abase.MainActivity" />

        <!-- 参考Activity -->
        <activity android:name="com.example.tonjies.abase.module.refrence.ReferenceActivity" />

        <!-- 带文字进度条示例 -->
        <activity android:name="com.example.tonjies.abase.module.tprogress.TProgressActivity" >
        </activity>

        <!-- 简单进度条示例 -->
        <activity android:name="com.example.tonjies.abase.module.easy.EasyActivity" />

        <!-- 完整diamante -->
        <activity android:name="com.example.tonjies.abase.module.example.ExampleActivity" />

        <!-- 带文字的进度条 -->
        <activity android:name="com.example.tonjies.abase.module.aeasy.AeasyActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>